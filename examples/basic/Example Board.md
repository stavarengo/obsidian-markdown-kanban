---
folia-board: true
card-folder: examples/basic/Cards
columns:
  - todo
  - doing
  - done
---

# Example Board

A minimal board to try the plugin. With the repository open as a vault, focus this note and run **“Open Folia Kanban board”**. Cards live in `examples/basic/Cards` — the `card-folder`, which is resolved relative to the **vault root** (not to this note). Columns are the `columns` list above. For a board that exercises every feature, see `examples/feature-showcase/`.
