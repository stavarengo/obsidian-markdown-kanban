{
  "$description": "Spacing scale. All live --folia-* tokens; values byte-match the .folia-root block in src/styles.css.",
  "spacing": {
    "$type": "dimension",
    "gap": {
      "$value": "10px",
      "$extensions": { "folia": { "cssVar": "--folia-gap", "live": true } }
    },
    "pad": {
      "$value": "12px",
      "$extensions": { "folia": { "cssVar": "--folia-pad", "live": true } }
    },
    "statusbar-clearance": {
      "$description": "Fallback only; App.tsx measures the real status bar and sets it live inline.",
      "$value": "32px",
      "$extensions": { "folia": { "cssVar": "--folia-statusbar-clearance", "live": true } }
    }
  }
}
