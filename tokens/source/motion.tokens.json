{
  "$description": "Motion durations and easing. All live --folia-* tokens; values byte-match the .folia-root block in src/styles.css. (The raw 220ms toast duration at styles.css:91 is NOT a --folia-* token and is intentionally not captured here so the block mapping stays exact.)",
  "motion": {
    "duration": {
      "$type": "duration",
      "fast": {
        "$value": "110ms",
        "$extensions": { "folia": { "cssVar": "--folia-dur-fast", "live": true } }
      },
      "default": {
        "$value": "170ms",
        "$extensions": { "folia": { "cssVar": "--folia-dur", "live": true } }
      }
    },
    "easing": {
      "default": {
        "$value": "cubic-bezier(0.16, 1, 0.3, 1)",
        "$type": "cubicBezier",
        "$extensions": { "folia": { "cssVar": "--folia-ease", "live": true } }
      }
    }
  }
}
