{
  "$description": "Corner radius scale. sm/md/lg/xl are live --folia-* tokens (byte-match the .folia-root block). pill is a NEW token home for the raw 999px used ~10x outside the block (no --folia-* var yet).",
  "radius": {
    "$type": "dimension",
    "sm": {
      "$value": "6px",
      "$extensions": { "folia": { "cssVar": "--folia-r-sm", "live": true } }
    },
    "md": {
      "$value": "8px",
      "$extensions": { "folia": { "cssVar": "--folia-r", "live": true } }
    },
    "lg": {
      "$value": "10px",
      "$extensions": { "folia": { "cssVar": "--folia-r-lg", "live": true } }
    },
    "xl": {
      "$value": "14px",
      "$extensions": { "folia": { "cssVar": "--folia-r-xl", "live": true } }
    },
    "pill": {
      "$description": "Fully-rounded pill radius. NEW home — raw 999px outside the block (CSS adoption pending).",
      "$value": "999px",
      "$extensions": { "folia": { "cssVar": "--folia-r-pill", "live": true } }
    }
  }
}
